<h2>sl-split-grid</h2>
<p class="lead">A dual-pane, master-detail split panel grid.</p>

<hr>

<h3>Example</h3>

<div class="row">
    <div class="col-lg-6">
        <h6>Template</h6>
        <pre>\{{sl-split-grid
    autoHeight=false
    columns=columns
    content=content
    detailName="color"
    detailTitlePath="name"
    title="Colors"
}}</pre>

        <h6>Route</h6>
        <pre>model: [
    {
        name    : 'Red',
        fruit   : 'Apple',
        hexCode : '#FF0000'
    }, {
        name    : 'Orange',
        fruit   : 'Orange',
        hexCode : '#FF7700'
    }, {
        name    : 'Yellow',
        fruit   : 'Banana',
        hexCode : '#FFFF00'
    }, {
        name    : 'Green',
        fruit   : 'Lime',
        hexCode : '#00FF00'
    }, {
        name    : 'Blue',
        fruit   : 'Blueberry',
        hexCode : '#0000FF'
    }, {
        name    : 'Purple',
        fruit   : 'Grape',
        hexCode : '#FF00FF'
    }
]</pre>

        <h6>Controller</h6>
        <pre>columns: [
    {
        title     : 'Color',
        valuePath : 'name'
    }, {
        size      : 'small',
        title     : 'Fruit',
        valuePath : 'fruit'
    }, {
        size      : 'small',
        title     : 'Hex Code',
        valuePath : 'hexCode'
    }
]</pre>
    </div>

    <div class="col-lg-6">
        <h6>Rendered Component</h6>
        {{sl-split-grid
            autoHeight=false
            columns=columns
            content=content
            detailName="color"
            detailTitlePath="name"
            title="Colors"
        }}
    </div>
</div>

<hr>

<h3>Properties</h3>

<div class="list-group">
    {{#property-text name="columns" type="array" required=true}}
        <p>An array of column definition objects.</p>

        <p>Each column definition object can contain the following values:
            <ul>
                <li><strong>size</strong> - Either a string for relative sizing ("small", "medium", or "large"), or a number for exact number of pixels</li>
                <li><strong>template</strong> - The name of a template to use for the cell value, for more complex content</li>
                <li><strong>title</strong> - The displayed title of the column</li>
                <li><strong>valuePath</strong> - The lookup string for the column's basic output; only used if <strong>template</strong> is omitted</li>
            </ul>
        </p>
    {{/property-text}}

    {{#property-text name="content" type="array" required=true}}
        Array of records to display as the rows.
    {{/property-text}}

    {{#property-text name="detailName" type="string" required=true}}
        The name of the template to render for the detail pane.
    {{/property-text}}

    {{#property-text name="actionButtonLabel" type="string" default="\"Actions\""}}
        The text to label the row actions' button with.
    {{/property-text}}

    {{#property-text name="autoHeight" type="boolean" default="true"}}
        Determines whether the content areas of the split-grid will dynamically calculate their allowed heights within the viewport when the window is resized. When false, the <code>height</code> property is used to determine the content areas' heights manually.
    {{/property-text}}

    {{#property-text name="detailFooterTemplate" type="string"}}
        The name of a template to use for the footer section of the detail pane. This template's controller is bound to the controller matching the <code>detailName</code> property.
    {{/property-text}}

    {{#property-text name="detailHeaderTemplate" type="string"}}
        The name of a template to use for the header section of the detail pane. If this property is not set, then the split-grid will attempt to use <code>detailTitlePath</code> to populate the detail title from the row record. This template's controller is bound to the controller matching the <code>detailName</code> property.
    {{/property-text}}

    {{#property-text name="detailTitlePath" type="string"}}
        The lookup property for the name of the record's property, to display as the title of the detail pane. This is only used if the <code>detailHeaderTemplate</code> is not set.
    {{/property-text}}

    {{#property-text name="filterName" type="string"}}
        The name of the controller/template to render for the filter section. If this is omitted, no filter button or interface will be created for the split-grid.
    {{/property-text}}

    {{#property-text name="headerTemplate" type="string"}}
        The name of a template to use for the split-grid's header section. If this property is not set, then the split-grid will use the <code>title</code> property to populate the main title text.
    {{/property-text}}

    {{#property-text name="height" type="number" default="600"}}
        Determines the content areas' heights in pixels. <span class="warning">Will only be used when <code>autoHeight</code> is <em>false</em></span>.
    {{/property-text}}

    {{#property-text name="rowActions" type="array"}}
        An array of action names, displayed in an "Actions" that will be fired on the <code>rowController</code> per record row.
    {{/property-text}}

    {{#property-text name="rowController" type="string"}}
        The name of an ObjectController to use as each row's itemController. This controller should contain the bound rowActions' functions in its <em>actions</em> hash.
    {{/property-text}}

    {{#property-text name="title" type="string"}}
        The title of the split-grid. This value is only used if <code>headerTemplate</code> is not set.
    {{/property-text}}
</div>
