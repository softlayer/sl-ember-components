{{sl-split-grid
    columns=columns
    content=content
    detailName="split-grid/details"
    detailFooterTemplate="split-grid/detail-footer"
    detailHeaderTemplate="split-grid/detail-header"
    filterName="split-grid/filter"
    headerTemplate="split-grid/header"
    listFooterTemplate="split-grid/list-footer"
    nextPage="nextPage"
    rowActions=gridRowActions
    rowController="split-grid/row"

    count=count
    totalCount=totalCount
}}
