<ul class="list-inline">
    {{#each items as |item|}}
        {{sl-menu-item
            action="handleAction"
            item=item
        }}
    {{/each}}

    {{#if allowShowAll}}
        {{sl-menu-item-show-all
            onMouseEnter="showAll"
        }}
    {{/if}}
</ul>
