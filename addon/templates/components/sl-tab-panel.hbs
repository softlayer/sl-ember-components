<ul class="nav nav-tabs" role="tablist">
    {{#each tab in tabs}}
        <li {{bind-attr class=":tab tab.active" data-tab-name=tab.name}}>
            <a {{action "change" tab.name}} role="tab">{{tab.label}}</a>
        </li>
    {{/each}}
</ul>

<div class="tab-content">
    {{yield}}
</div>
