{{#if label}}
    <label for={{sl-select-view.elementId}} class="control-label">
        {{label}}

        {{#if optional}}
            <small class="text-info">Optional</small>
        {{/if}}

        {{#if required}}
            <small class="text-danger">Required</small>
        {{/if}}
    </label>
{{/if}}

{{input
    viewName="sl-select-view"
    type="hidden"
    class="form-control"
    disabled=disabledString
    readonly=readonlyString
    value=value
}}

{{#if helpText}}
    <p class="help-block">{{helpText}}</p>
{{/if}}
