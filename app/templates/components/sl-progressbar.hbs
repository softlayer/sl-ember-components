<div aria-valuemin="0" aria-valuemax="100" {{bind-attr aria-valuenow=value class=barClass style=styleString}} role="progressbar">
    {{#if label}}
        {{value}}%
    {{else}}
        <span class="sr-only">{{value}}% Complete</span>
    {{/if}}
</div>
