<header class="grid-header">
    <div class="row">
        <div class="col-sm-6">
            <h3 class="title">
                {{partial headerPath}}
            </h3>
        </div>

        {{#if showFilter}}
            <div class="col-sm-6 text-right">
                {{#sl-button action="toggleFilterPane" size="small"}}{{filterText}}{{/sl-button}}
            </div>
        {{/if}}
    </div>
</header>

<div class="filter-pane">
    {{#if filterOpen}}
        <div class="filter-content">
            {{render-dynamic filterPath}}
        </div>
    {{/if}}
</div>

<div class="list-pane">
    <table class="table column-headers">
        <thead>
            <tr>
                {{#each column in columns itemViewClass="sl-split-grid-column-header"}}
                    {{column.title}}
                    {{#if column.sortable}}
                        <i {{bind-attr class=":fa view.sortIconClass"}}></i>
                    {{/if}}
                {{/each}}

                {{#if showActions}}
                    <th class="actions-cell"></th>
                {{/if}}
            </tr>
        </thead>
    </table>

    <div class="content">
        <table class="table table-hover table-striped">
            <tbody>
                {{#each row in content itemController=detailPath itemViewClass="sl-split-grid-row"}}
                    {{#each column in columns itemViewClass="sl-split-grid-cell"}}
                        {{split-grid-cell}}
                    {{/each}}

                    {{#if showActions}}
                        <td class="actions-cell">
                            {{#sl-drop-button align="right" label=actionsButtonLabel size="small" theme="hover"}}
                                {{#each rowAction in row.rowActions}}
                                    {{sl-drop-option action=rowAction.action label=rowAction.label}}
                                {{/each}}
                            {{/sl-drop-button}}
                        </td>
                    {{/if}}
                {{/each}}
            </tbody>
        </table>
    </div>

    <footer>
        <div class="row">
            <div class="col-xs-6">
                {{#if footerPath}}
                    {{partial footerPath}}
                {{/if}}
            </div>
            <div class="col-xs-6 text-right">
                {{#if pendingData}}
                    {{sl-loading-icon}}
                {{/if}}
            </div>
        </div>
    </footer>
</div>

<div class="detail-pane">
    <header>
        <span class="title">
            {{render-dynamic detailHeaderPath activeRecord context=detailPath}}
        </span>

        <button {{action "closeDetailPane"}} class="close">&times;</button>
    </header>

    <div class="content">
        <div class="detail-content">
            {{render-dynamic detailPath activeRecord}}
        </div>
    </div>

    <footer>
        {{render-dynamic detailFooterPath activeRecord context=detailPath}}
    </footer>
</div>
